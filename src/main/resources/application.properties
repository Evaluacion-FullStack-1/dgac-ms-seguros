spring.application.name=dgac-ms-seguros

# Puerto dinámico para la nube, y 8084 por defecto en tu entorno local
server.port=${PORT:8084}

# Conexión a la base de datos de Neon parametrizada
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-long-rice-ap8b14l5-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_TpzSQ54uhZlH}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ==============================================================
# CONFIGURACIÓN DE INTEGRACIÓN CON OTROS MICROSERVICIOS
# ==============================================================
# URL base para comunicarse con el MS de Drones
drones.base-url=http://dgac-ms-drones